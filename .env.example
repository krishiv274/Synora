# Synora — copy to ".env" and load before running (never commit real keys).
#   cp .env.example .env
#   export $(grep -v '^#' .env | xargs)   # macOS/Linux; or use python-dotenv

# --- Agentic Planner (at least one recommended) ---
GROQ_API_KEY=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# groq | anthropic | openai
MODEL_PROVIDER=groq
